# —— 环境与密钥 ——
.env
.envrc

# —— Python ——
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# —— uv / venv ——
.venv/
venv/

# —— LangGraph Studio 本地缓存 ——
.langgraph_api/

# —— Demo / Test workspace (每次 demo 启动从 template 重置, 不入 git) ——
tests/fixtures/_buggy_ik_workspace.py

# —— M1 benchmark 可重复运行产物 ——
benchmarks/workspaces/
benchmarks/results/
benchmarks/rag_results/

# —— Checkpoint / RAG 持久化 (Phase 4+) ——
*.sqlite
*.db
chroma_db/
.agent_state/

# —— 编辑器 ——
.vscode/
.idea/
*.swp
.DS_Store
