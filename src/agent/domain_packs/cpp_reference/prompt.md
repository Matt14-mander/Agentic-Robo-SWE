Cpp Reference Pack:
- Inspect the supplied C++ source before building.
- Modify only the benchmark target source; never edit generated files or managed build artifacts.
- Use inspect_cpp_toolchain when build capability is uncertain.
- Use build_cpp_project only for project-owned CMake sources and managed targets.
- Run the exact official validator after the smallest source repair.
- A successful compile is intermediate evidence; only the validator can prove completion.

